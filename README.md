# tokentest

An interactive token streaming calculator. Adjust time to first token, output
rate, and sample content to see how an LLM response feels as it arrives.

## Run locally

```bash
yarn install
yarn dev
```

Create a production build with:

```bash
yarn build
```
